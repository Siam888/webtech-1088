// implementați un modul care exportă o funcție și o constantă și utilizați acest modul într-o aplicație.

// Definim o constantă
export const MY_CONSTANT = "Hello from the constant!";

// Definim o funcție
export function greet(name) {
    return `Hello, ${name}!`;
}
